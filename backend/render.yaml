services:
  - type: web
    name: smart-coding-interview-node-backend
    runtime: docker # Changed from node to enable C++ (g++) support via Docker
    envVars:
      - key: PORT
        value: 10000
      - key: GROQ_API_KEY
        sync: false
      - key: MONGODB_URI
        sync: false
